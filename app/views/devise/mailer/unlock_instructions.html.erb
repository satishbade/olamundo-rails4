<p><%= t('hello') %> <%= @resource.email %>!</p>

<p><%=  t('ui.h1.account_locked') %></p>

<p><%= t('ui.h1.link_for_unlock_account') %></p>

<p><%= link_to t('ui.action.unlock_account'), unlock_url(@resource, :unlock_token => @resource.unlock_token) %></p>
